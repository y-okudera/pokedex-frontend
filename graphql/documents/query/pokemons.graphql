query ListPokemons($limit: Int!, $offset: Int!) {
  pokemons(limit: $limit, offset: $offset) {
    count
    nextOffset
    results {
      id
      name
      image
      artwork
      dreamworld
    }
  }
}
